<div class="container">
<h1 class="display-3 text-center">Registrar atención</h1>
  <div class="form-group">
    <%= form_for :appointment, :url => attended_path(@appointment, @current_user.id), :method => :patch do |f| %>
      <% @appointment.errors.full_messages.each do |error| %>
        <div>
          <% error %>
        </div>
      <% end %>
      <div>
        <%= f.label :Comentario %>
        <%= f.text_field :comment, class: 'form-control', required: true %>
      </div>
      <div>
        <%= f.submit 'Guardar', :class => 'btn btn-success' %>
      </div>
    <% end %>
  </div>
  <div><%= link_to 'volver', appointments_path %></div>
</div>
